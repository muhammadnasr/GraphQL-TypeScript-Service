// This constant represents the path to the resolvers
export const RESOLVERS_PATH = `${__dirname}/../resolvers/**/*Resolver.ts`;

// This constant represents the path to the entities
export const ENTITIES_PATH = `src/entities/*.ts`;
